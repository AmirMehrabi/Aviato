@extends('layouts.admin')
@section('title', 'ردپای مدیریتی')
@section('content')
<div class="px-4 py-6 md:px-8 lg:px-10"><h1 class="text-2xl font-black">ردپای مدیریتی</h1><p class="mt-2 text-sm text-slate-500">سابقه تغییرناپذیر ورودها، تلاش‌های ردشده و عملیات پنل</p>
<form class="mt-6 grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 md:grid-cols-5"><select name="user_id" class="rounded-xl border border-slate-200 px-3 py-3"><option value="">همه کاربران</option>@foreach($users as $id=>$name)<option value="{{ $id }}" @selected((string)request('user_id')===(string)$id)>{{ $name }}</option>@endforeach</select><input name="event" value="{{ request('event') }}" placeholder="نام رویداد" class="rounded-xl border border-slate-200 px-3 py-3" dir="ltr"><select name="result" class="rounded-xl border border-slate-200 px-3 py-3"><option value="">همه نتیجه‌ها</option>@foreach(['success'=>'موفق','failed'=>'ناموفق','denied'=>'ردشده'] as $value=>$label)<option value="{{ $value }}" @selected(request('result')===$value)>{{ $label }}</option>@endforeach</select><input type="date" name="from" value="{{ request('from') }}" class="rounded-xl border border-slate-200 px-3 py-3"><button class="rounded-xl bg-slate-950 px-4 py-3 text-sm font-black text-white">فیلتر</button></form>
<div class="mt-5 overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-xs text-slate-500"><tr><th class="px-5 py-3 text-right">زمان</th><th>کاربر</th><th>رویداد</th><th>نتیجه</th><th>IP</th><th></th></tr></thead><tbody class="divide-y">@forelse($logs as $log)<tr><td class="px-5 py-4">{{ \Morilog\Jalali\Jalalian::fromCarbon($log->created_at)->format('Y/m/d H:i:s') }}</td><td>{{ $log->actor_name ?: 'ناشناس' }}<p class="text-xs text-slate-400" dir="ltr">{{ $log->actor_email }}</p></td><td dir="ltr">{{ $log->event }}</td><td><span class="rounded-full px-3 py-1 text-xs font-black {{ $log->result==='success'?'bg-emerald-50 text-emerald-700':($log->result==='denied'?'bg-amber-50 text-amber-700':'bg-red-50 text-red-700') }}">{{ $log->result }}</span></td><td dir="ltr">{{ $log->ip_address }}</td><td class="px-5"><a class="font-black text-[#0069FF]" href="{{ route('admin.audit.show',$log) }}">جزئیات</a></td></tr>@empty<tr><td colspan="6" class="p-12 text-center text-slate-500">رویدادی پیدا نشد.</td></tr>@endforelse</tbody></table><div class="border-t p-4">{{ $logs->links() }}</div></div></div>
@endsection
