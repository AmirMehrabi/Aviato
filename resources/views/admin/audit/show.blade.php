@extends('layouts.admin')
@section('title', 'جزئیات رویداد')
@section('content')
<div class="px-4 py-6 md:px-8 lg:px-10"><a href="{{ route('admin.audit.index') }}" class="text-sm font-black text-[#0069FF]">بازگشت</a><h1 class="mt-3 text-2xl font-black" dir="ltr">{{ $auditLog->event }}</h1><div class="mt-6 grid gap-5 lg:grid-cols-2"><section class="rounded-2xl border border-slate-200 bg-white p-5"><h2 class="font-black">فراداد درخواست</h2><dl class="mt-5 space-y-3 text-sm">@foreach(['کاربر'=>$auditLog->actor_name,'ایمیل'=>$auditLog->actor_email,'متد'=>$auditLog->method,'مسیر'=>$auditLog->path,'نتیجه'=>$auditLog->result,'کد HTTP'=>$auditLog->status_code,'IP'=>$auditLog->ip_address,'Request ID'=>$auditLog->request_id] as $label=>$value)<div class="flex justify-between gap-4"><dt class="text-slate-500">{{ $label }}</dt><dd class="break-all text-left font-bold" dir="ltr">{{ $value ?: '—' }}</dd></div>@endforeach</dl></section><section class="rounded-2xl border border-slate-200 bg-white p-5"><h2 class="font-black">هدف</h2><dl class="mt-5 space-y-3 text-sm"><div class="flex justify-between"><dt class="text-slate-500">Type</dt><dd dir="ltr">{{ $auditLog->target_type ?: '—' }}</dd></div><div class="flex justify-between"><dt class="text-slate-500">ID</dt><dd dir="ltr">{{ $auditLog->target_id ?: '—' }}</dd></div><div><dt class="text-slate-500">User agent</dt><dd class="mt-2 break-all text-xs" dir="ltr">{{ $auditLog->user_agent }}</dd></div></dl></section></div><div class="mt-5 grid gap-5 lg:grid-cols-2"><section class="rounded-2xl border border-slate-200 bg-white p-5"><h2 class="font-black">Metadata</h2><pre class="mt-4 overflow-auto rounded-xl bg-slate-950 p-4 text-xs text-slate-100" dir="ltr">{{ json_encode($auditLog->metadata, JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE) }}</pre></section><section class="rounded-2xl border border-slate-200 bg-white p-5"><h2 class="font-black">Changes</h2><pre class="mt-4 overflow-auto rounded-xl bg-slate-950 p-4 text-xs text-slate-100" dir="ltr">{{ json_encode($auditLog->changes, JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE) }}</pre></section></div></div>
@endsection
