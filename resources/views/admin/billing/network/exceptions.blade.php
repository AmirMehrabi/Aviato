@extends('layouts.admin')
@section('title', 'استثناهای شبکه')
@section('content')
<div class="px-4 py-6 md:px-8 lg:px-10"><h1 class="text-2xl font-black">استثناها و داده ناقص</h1><p class="mt-2 text-sm text-slate-500">داده ناقص یا قرنطینه‌شده صفر محسوب و قیمت‌گذاری نمی‌شود.</p><div class="mt-5">@include('admin.billing.network._nav')</div>@if(session('status'))<div class="mb-4 rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-700">{{ session('status') }}</div>@endif @if(session('error'))<div class="mb-4 rounded-xl bg-red-50 p-4 text-sm font-bold text-red-700">{{ session('error') }}</div>@endif<div class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div class="overflow-x-auto"><table class="w-full text-right text-sm"><thead class="bg-slate-50 text-xs text-slate-500"><tr><th class="px-5 py-3">باکت</th><th class="px-5 py-3">ماشین</th><th class="px-5 py-3">بازه</th><th class="px-5 py-3">وضعیت</th><th class="px-5 py-3">علت</th><th class="px-5 py-3">عملیات</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($buckets as $bucket)<tr><td class="px-5 py-4 font-mono text-xs" dir="ltr">{{ $bucket->bucket_id }}<br>rev {{ $bucket->revision }}</td><td class="px-5 py-4">{{ $bucket->virtualMachine?->display_name ?? 'UUID ناشناخته' }}<p class="text-xs text-slate-400" dir="ltr">{{ $bucket->vm_uuid }}</p></td><td class="px-5 py-4 text-xs" dir="ltr">{{ $bucket->interval_start }}<br>{{ $bucket->interval_end }}</td><td class="px-5 py-4"><span class="rounded-full bg-amber-50 px-3 py-1 text-xs font-black text-amber-700">{{ $bucket->completeness }} / {{ $bucket->processing_status }}</span></td><td class="max-w-sm px-5 py-4 text-red-700">{{ $bucket->processing_error ?: 'IPDR داده را کامل اعلام نکرده است.' }}</td><td class="px-5 py-4">@if($bucket->virtual_machine_id)<form method="POST" action="{{ route('admin.billing.network.exceptions.retry', $bucket) }}">@csrf<button class="rounded-lg border border-slate-200 px-3 py-2 text-xs font-black text-[#0069FF]">تلاش دوباره</button></form>@else<span class="text-xs text-slate-400">نیازمند اصلاح IPDR</span>@endif</td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-emerald-600">هیچ استثنای بازی وجود ندارد.</td></tr>@endforelse</tbody></table></div><div class="border-t px-5 py-4">{{ $buckets->links() }}</div></div></div>
@endsection
