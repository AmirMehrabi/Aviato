@extends('layouts.admin')
@section('title', 'تطبیق IPDR')
@section('content')
<div class="px-4 py-6 md:px-8 lg:px-10"><h1 class="text-2xl font-black">تطبیق مستقل IPDR</h1><p class="mt-2 text-sm text-slate-500">مجموع باکت‌های پذیرفته‌شده Aviato با endpoint مستقل Summary مقایسه می‌شود.</p><div class="mt-5">@include('admin.billing.network._nav')</div><form method="POST" action="{{ route('admin.billing.network.reconcile') }}" class="mb-6 grid gap-4 rounded-2xl border border-slate-200 bg-white p-5 md:grid-cols-4">@csrf<input required name="from" type="datetime-local" value="{{ old('from') }}" class="rounded-xl border border-slate-200 px-4 py-3" dir="ltr"><input required name="to" type="datetime-local" value="{{ old('to') }}" class="rounded-xl border border-slate-200 px-4 py-3" dir="ltr"><input name="vm_uuid" value="{{ old('vm_uuid') }}" placeholder="VM UUID (اختیاری)" class="rounded-xl border border-slate-200 px-4 py-3" dir="ltr"><button class="rounded-xl bg-[#0069FF] px-4 py-3 text-sm font-black text-white">اجرای تطبیق</button></form><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><table class="w-full text-right text-sm"><thead class="bg-slate-50 text-xs text-slate-500"><tr><th class="px-4 py-3">VM UUID</th><th class="px-4 py-3">RX IPDR / Aviato</th><th class="px-4 py-3">TX IPDR / Aviato</th><th class="px-4 py-3">باکت‌ها</th><th class="px-4 py-3">نتیجه</th></tr></thead><tbody class="divide-y">@forelse($rows as $row)<tr><td class="px-4 py-4 font-mono text-xs">{{ $row['vm_uuid'] }}</td><td class="px-4 py-4" dir="ltr">{{ number_format($row['remote_ingress_bytes']) }} / {{ number_format($row['local_ingress_bytes']) }}</td><td class="px-4 py-4" dir="ltr">{{ number_format($row['remote_egress_bytes']) }} / {{ number_format($row['local_egress_bytes']) }}</td><td class="px-4 py-4" dir="ltr">{{ $row['remote_bucket_count'] }} / {{ $row['local_bucket_count'] }}</td><td class="px-4 py-4"><span class="rounded-full px-3 py-1 text-xs font-black {{ $row['matches'] ? 'bg-emerald-50 text-emerald-700' : 'bg-red-50 text-red-700' }}">{{ $row['matches'] ? 'مطابق' : 'اختلاف' }}</span></td></tr>@empty<tr><td colspan="5" class="px-5 py-12 text-center text-slate-500">یک بازه انتخاب و تطبیق را اجرا کنید.</td></tr>@endforelse</tbody></table></div></div>
@endsection
