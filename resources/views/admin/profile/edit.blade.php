@extends('layouts.admin')
@section('title', 'حساب من')
@section('content')
<div class="px-4 py-6 md:px-8 lg:px-10"><div class="mx-auto max-w-2xl"><h1 class="text-2xl font-black">حساب من</h1><section class="mt-6 rounded-2xl border border-slate-200 bg-white p-6"><div class="flex items-center gap-4"><span class="grid size-14 place-items-center rounded-full bg-[#0069FF] text-xl font-black text-white">{{ mb_substr($user->name,0,1) }}</span><div><h2 class="font-black">{{ $user->name }}</h2><p class="text-sm text-slate-500">{{ $user->role->label() }} · {{ $user->email ?: $user->phone }}</p></div></div></section>@if(session('status'))<div class="mt-5 rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-700">{{ session('status') }}</div>@endif@if($errors->any())<div class="mt-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">{{ $errors->first() }}</div>@endif<section class="mt-5 rounded-2xl border border-slate-200 bg-white p-6"><h2 class="font-black">تغییر رمز عبور</h2><form method="POST" action="{{ route('admin.profile.password.update') }}" class="mt-5 space-y-4">@csrf @method('PATCH')<input name="current_password" type="password" required placeholder="رمز فعلی" class="w-full rounded-xl border border-slate-200 px-4 py-3" dir="ltr"><input name="password" type="password" required placeholder="رمز جدید" class="w-full rounded-xl border border-slate-200 px-4 py-3" dir="ltr"><input name="password_confirmation" type="password" required placeholder="تکرار رمز جدید" class="w-full rounded-xl border border-slate-200 px-4 py-3" dir="ltr"><button class="rounded-xl bg-[#0069FF] px-6 py-3 text-sm font-black text-white">تغییر رمز</button></form></section></div></div>
@endsection
