@extends('layouts.admin')
@section('title','دسترسی پروموشن')
@section('content')<div class="px-4 py-6 md:px-8 lg:px-10"><h1 class="text-2xl font-black">دسترسی مدیریت پروموشن</h1><p class="mt-2 text-sm text-slate-500">فقط ایمیل‌های super-admin تنظیم‌شده در محیط می‌توانند این دسترسی را تغییر دهند.</p><div class="mt-5 overflow-x-auto rounded-xl border bg-white"><table class="min-w-full text-sm"><thead class="bg-slate-50"><tr><th class="p-4 text-right">مدیر</th><th>Super-admin</th><th>مدیریت پروموشن</th><th></th></tr></thead><tbody class="divide-y">@foreach($users as $user)<tr><td class="p-4"><b>{{ $user->name }}</b><p class="text-xs text-slate-400">{{ $user->email }}</p></td><td>{{ $user->isPromotionSuperAdmin()?'بله':'خیر' }}</td><td>{{ $user->can_manage_promotions?'فعال':'غیرفعال' }}</td><td><form method="POST" action="{{ route('admin.promotion-users.update',$user) }}">@csrf @method('PATCH')<input type="hidden" name="can_manage_promotions" value="{{ $user->can_manage_promotions?0:1 }}"><button class="rounded-lg px-4 py-2 font-black {{ $user->can_manage_promotions?'bg-rose-50 text-rose-700':'bg-emerald-50 text-emerald-700' }}">{{ $user->can_manage_promotions?'لغو دسترسی':'اعطای دسترسی' }}</button></form></td></tr>@endforeach</tbody></table><div class="border-t p-4">{{ $users->links() }}</div></div></div>@endsection
