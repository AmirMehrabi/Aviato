@extends('customer.layout')
@section('title', 'مصرف شبکه')
@section('header_title', 'مصرف شبکه')
@section('header_subtitle', 'سهمیه، مصرف نهایی‌شده و هزینه تخمینی ماشین‌های فضای کاری')
@php($activeNav = 'network')
@section('content')
<div class="grid gap-4 lg:grid-cols-2 xl:grid-cols-3">@forelse($vms as $vm)@php($s=$summaries[$vm->id])<article class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-start justify-between"><div><h2 class="font-black text-slate-950">{{ $vm->display_name }}</h2><p class="mt-1 font-mono text-xs text-slate-400" dir="ltr">{{ $vm->ip_address ?: $vm->uuid }}</p></div><span class="rounded-full px-3 py-1 text-xs font-black {{ $s['freshness']['tone']==='healthy' ? 'bg-emerald-50 text-emerald-700' : ($s['freshness']['tone']==='danger' ? 'bg-red-50 text-red-700' : 'bg-amber-50 text-amber-700') }}">{{ $s['freshness']['label'] }}</span></div>@if($s['period'])<div class="mt-5 flex items-end justify-between"><div><p class="text-xs text-slate-500">مصرف محاسبه‌شده</p><p class="mt-1 text-xl font-black" dir="ltr">{{ $reports->bytes($s['rated_bytes']) }}</p></div><p class="text-sm font-black text-[#0069FF]">{{ $wallets->format($s['accrued_amount']) }}</p></div><div class="mt-4 h-2 overflow-hidden rounded-full bg-slate-100"><div class="h-full {{ ($s['usage_percent']??100)>=100 ? 'bg-amber-500':'bg-[#0069FF]' }}" style="width:{{ min(100,$s['usage_percent']??100) }}%"></div></div><p class="mt-2 text-xs text-slate-500">{{ $s['usage_percent'] === null ? 'بدون سهمیه رایگان' : $s['usage_percent'].'٪ از '.$reports->bytes($s['included_bytes']) }}</p>@else<p class="mt-6 rounded-xl bg-slate-50 p-4 text-sm text-slate-500">هنوز داده نهایی دریافت نشده است؛ این وضعیت به معنی مصرف صفر نیست.</p>@endif<a href="{{ route('customer.network.show', $vm, false) }}" class="mt-5 flex min-h-11 items-center justify-center rounded-xl border border-[#B8D6FF] bg-[#EBF3FF] text-sm font-black text-[#0069FF]">جزئیات مصرف</a></article>@empty<div class="col-span-full rounded-2xl border border-dashed border-slate-300 bg-white py-16 text-center text-slate-500">ماشین قابل مشاهده‌ای در این فضای کاری وجود ندارد.</div>@endforelse</div>
@endsection
