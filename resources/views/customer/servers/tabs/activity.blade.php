<div class="space-y-5"><div><h2 class="text-xl font-black text-slate-950">فعالیت سرور</h2><p class="mt-1 text-sm text-slate-500">درخواست‌ها و نتیجه عملیات ثبت شده از زمان فعال شدن این تاریخچه</p></div>
    <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><ol class="divide-y divide-slate-100">@forelse ($activities as $activity)<li class="flex gap-4 p-5"><span class="mt-1 grid size-9 shrink-0 place-items-center rounded-full {{ $activity->outcome === 'failed' ? 'bg-red-50 text-red-600' : ($activity->outcome === 'succeeded' ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700') }}"><svg class="size-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M12 3v9l6 3m4-3a10 10 0 1 1-20 0 10 10 0 0 1 20 0Z"/></svg></span><div class="min-w-0 flex-1"><div class="flex flex-wrap items-center justify-between gap-2"><h3 class="font-black text-slate-950">{{ $activity->title }}</h3><time class="text-xs text-slate-500" datetime="{{ $activity->created_at->toIso8601String() }}">{{ \App\Support\Jalali::format($activity->created_at) }}</time></div>@if($activity->detail)<p class="mt-2 text-sm leading-6 text-slate-600">{{ $activity->detail }}</p>@endif</div></li>@empty<li class="p-8 text-center text-sm text-slate-500">هنوز فعالیتی برای این سرور ثبت نشده است. رویدادهای پیش از فعال شدن این تاریخچه بازسازی نمی‌شوند.</li>@endforelse</ol></section>
    @if ($activities->hasPages())<div>{{ $activities->links() }}</div>@endif
</div>
